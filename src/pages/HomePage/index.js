import React, {Component} from 'react';
import DynamicTabNav from '../../navigator/DynamicTavNavigator';

export default class extends Component {
  // static router = DynamicTabNav.router;

  render() {
    return <DynamicTabNav></DynamicTabNav>;
  }
}
